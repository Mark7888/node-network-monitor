# Production environment variables for Docker
# Used by docker-entrypoint.sh to generate runtime config

# API Server URL
API_URL=http://localhost:8080

# Refresh interval (milliseconds)
REFRESH_INTERVAL=10000

# Chart animation
ENABLE_CHART_ANIMATION=false

# Debug mode
DEBUG=false
